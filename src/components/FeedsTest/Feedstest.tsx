import { useAppSelector } from "../../hooks";

const Feedstest: React.FC = () => {
    // const { items } = useAppSelector((state) => state.sections);

    return <div>Меню с потоками</div>;
};

export { Feedstest };
